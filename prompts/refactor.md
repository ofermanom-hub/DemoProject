# Refactor Prompt

Refactor the following code without changing behavior:
- Extract pure logic into src/core/
- Move HTTP concerns to src/api/
- Move external I/O to src/integrations/
- Apply strict typing (no `Any`)
- Improve readability and reduce duplication

Show only the changed sections as diffs.
